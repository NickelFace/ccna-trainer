{"id":"sec-05-aaa","dom":"SEC","title":"AAA, RADIUS, TACACS+, and 802.1X","lead":"The three A's individually, how TACACS+ differs from RADIUS, and how 802.1X port authentication works.","minutes":16,"blueprint":["5.5"],"sections":[{"id":"три-a","title":"The three A's","blocks":[{"t":"table","head":["","Question","Example"],"rows":[["**Authentication**","Who are you?","username and password, certificate, token"],["**Authorization**","What are you allowed to do?","privilege level, set of permitted commands, VLAN"],["**Accounting**","What did you do?","command log, session duration, traffic volume"]]},{"t":"p","text":"The distinction is asked about directly: \"the user logged in\" is authentication; \"they were only permitted show commands\" is authorization; \"every command entered is logged\" is accounting."}]},{"id":"radius-и-tacacs","title":"RADIUS and TACACS+","blocks":[{"t":"table","head":["","RADIUS","TACACS+"],"rows":[["Standard","open (RFC)","Cisco"],["Transport","**UDP** 1812/1813 (or 1645/1646)","**TCP 49**"],["What's encrypted","only the password","**the entire packet**"],["A-A-A separation","authentication and authorization are **combined**","all three are **separate**"],["Typical use","user network access: 802.1X, Wi-Fi, VPN","device administration: who can enter which commands"]]},{"t":"p","text":"Selection mnemonic: **network access for users → RADIUS; equipment management → TACACS+** (that needs per-command authorization, which RADIUS can't do)."},{"t":"code","lang":"cfg","text":"aaa new-model\n!\nradius server ISE\n address ipv4 10.0.0.80 auth-port 1812 acct-port 1813\n key R@diusKey\n!\ntacacs server TAC1\n address ipv4 10.0.0.81\n key T@cacsKey\n!\naaa authentication login default group tacacs+ local\naaa authorization exec default group tacacs+ local\naaa accounting commands 15 default start-stop group tacacs+"},{"t":"note","kind":"key","title":"Remember","paras":["The keyword **`local` at the end** is a fallback login method for when the server is unreachable. Without it, an AAA server failure means nobody can log in, including the administrator."]}]},{"id":"802-1x-аутентификация-порта","title":"802.1X: port authentication","blocks":[{"t":"p","text":"The port doesn't pass traffic until the device proves who it is."},{"t":"p","text":"Three roles:"},{"t":"ul","items":["**Supplicant** — the client (the computer's OS, a phone);","**Authenticator** — the switch or access point, acting as an intermediary;","**Authentication server** — RADIUS (typically Cisco ISE)."]},{"t":"p","text":"Exchange: the client and server speak **EAP**, and the switch wraps EAP inside RADIUS. Before successful authentication, **only EAPOL** passes through the port."},{"t":"code","lang":"cfg","text":"aaa authentication dot1x default group radius\ndot1x system-auth-control\n!\ninterface GigabitEthernet1/0/5\n switchport mode access\n authentication port-control auto\n dot1x pae authenticator"},{"t":"p","text":"Beyond simply granting access, the server can also return a **dynamic VLAN**, ACL, or QoS profile — the device lands in exactly the network it's supposed to. For devices without a supplicant (printers, cameras), use **MAB** (MAC Authentication Bypass) or a guest VLAN."}]},{"id":"где-это-встречается-на-практике-уровня-c","title":"Where this shows up in practice at the CCNA level","blocks":[{"t":"ul","items":["Wi-Fi **WPA2/WPA3-Enterprise** — the same 802.1X, just over radio; on the WLC, RADIUS is selected under Security → AAA Servers.","Administrator login to switches and routers — TACACS+ with per-command authorization.","VPN access — RADIUS with MFA."]}]},{"id":"разбор-полный-обмен-802-1x-от-подключени","title":"Walkthrough: the full 802.1X exchange from cable connection to network access","blocks":[{"t":"code","lang":"txt","text":"1. Client plugs in the cable. Port is in the unauthorized state -- only EAPOL passes through.\n2. Supplicant -> Authenticator: EAPOL-Start (client announces it's ready to authenticate)\n3. Authenticator -> Supplicant: EAP-Request Identity\n4. Supplicant -> Authenticator: EAP-Response Identity (e.g., username)\n5. Authenticator wraps EAP inside a RADIUS Access-Request -> Authentication Server\n6. Authentication Server and Supplicant exchange EAP inside RADIUS\n   (the method depends on the EAP type: EAP-TLS -- certificates, PEAP -- username/password inside a TLS tunnel)\n7. Authentication Server -> Authenticator: RADIUS Access-Accept\n   (may carry VLAN assignment, ACL, session timeout -- RADIUS attributes)\n8. Authenticator moves the port to authorized -- normal traffic starts flowing"},{"t":"p","text":"The key detail for understanding the architecture: the **authenticator (the switch) never validates the password itself and doesn't store any accounts** — it only relays EAP messages between the supplicant and the authentication server, wrapping them in RADIUS. All the validation logic lives on the server. This explains why switching the authentication method (say, PEAP to EAP-TLS) requires changes on the client and the server, but not on the switch itself — it sees the same EAP frames regardless of their content."}]},{"id":"диагностика-802-1x-пускает-не-в-ту-vlan","title":"Diagnosis: 802.1X grants access to the wrong VLAN","blocks":[{"t":"p","text":"**Symptom.** A device successfully passes 802.1X authentication (the port moves to authorized), but ends up in the port's default VLAN instead of the expected corporate one."},{"t":"p","text":"**What to look at.** Whether the RADIUS server is returning VLAN assignment attributes in the Access-Accept, and whether the switch is applying them:"},{"t":"code","lang":"cli","text":"SW1# show authentication sessions interface gi1/0/5\n            Interface:  GigabitEthernet1/0/5\n                 Status:  Authz Success\n                    Vlan:  1"},{"t":"p","text":"**Conclusion.** Authentication succeeded (`Authz Success`), but the VLAN stayed at the default — which means either the RADIUS server isn't configured to send the `Tunnel-Type`/`Tunnel-Medium-Type`/`Tunnel-Private-Group-ID` attributes (these are exactly what carries the dynamic VLAN number in the Access-Accept), or the switch received them but couldn't apply them (for instance, no VLAN with that number exists on this switch). That splits the cause into two sides: first check the policy on the RADIUS server itself (the authorization profile for this user/device), then check whether the target VLAN exists and is active on the switch — exactly as in the VLAN chapter."}]},{"id":"диагностика-администратор-не-может-войти","title":"Diagnosis: an administrator can't log in because the TACACS+ server is unreachable","blocks":[{"t":"p","text":"**Symptom.** The primary TACACS+ server goes down (planned maintenance or a failure), and no administrator can log in to the switches — not even the ones who could log in fine before."},{"t":"p","text":"**What to look at.** The full `aaa authentication login` line, not just whether TACACS+ is configured:"},{"t":"code","lang":"cli","text":"SW1# show running-config | include aaa authentication login\naaa authentication login default group tacacs+"},{"t":"p","text":"**Conclusion.** The method list has **no `local`** at the end — when the `tacacs+` group is unreachable, authentication simply fails outright, with no fallback method. This is exactly the trap the callout above warns about: `group tacacs+ local` would mean \"try TACACS+ first, and if the server is unreachable, fall back to the local database,\" while a bare `group tacacs+` leaves no way back for anyone, including an administrator with console access. The fix is to add a fallback method, but that can only be done if there's at least one working way in (for example, via ROMMON, if there's truly no other option)."}]},{"id":"что-спрашивают","title":"What gets asked","blocks":[{"t":"ul","items":["\"Which protocol separates authentication and authorization?\" — TACACS+.","\"Which protocol encrypts the entire packet?\" — TACACS+ (RADIUS only encrypts the password).","\"Which transport and port does TACACS+ use?\" — TCP 49.","\"What is the role of the supplicant/authenticator/authentication server?\" — the client, the switch, RADIUS.","\"Which AAA element records what a user did?\" — accounting.","\"What happens if the AAA server is unreachable and no local fallback is configured?\" — login becomes impossible.","\"Does the switch itself validate the user's password during 802.1X?\" — no, the switch only wraps EAP inside RADIUS and relays it between the supplicant and the server; validation happens entirely on the authentication server.","\"A device passes 802.1X authentication but ends up in the default VLAN instead of the expected one. What should be checked?\" — whether the RADIUS server is sending the Tunnel-Type/Tunnel-Private-Group-ID attributes in the Access-Accept, and whether the specified VLAN exists on the switch.","\"Administrators cannot log in to switches after the TACACS+ server goes down. What is misconfigured?\" — the `local` fallback method is missing from `aaa authentication login`."]}]},{"id":"проверь-себя","title":"Check yourself","blocks":[{"t":"check","items":[{"q":"You need to restrict the set of commands a support engineer can run on switches. Which protocol?","a":"TACACS+ -- it supports per-command authorization and keeps the three A's separate."},{"q":"What passes through an 802.1X port before authentication succeeds?","a":"Only EAPOL frames; all other traffic is blocked."},{"q":"What transport does RADIUS use, and exactly what does it encrypt?","a":"UDP; only the password is encrypted, the rest of the packet is sent in the clear."},{"q":"How do you connect a printer that has no supplicant to the network?","a":"Via MAB -- MAC-address-based authentication -- or by placing the port in a guest VLAN."},{"q":"Why put local last in the aaa authentication login command?","a":"As a fallback: if the server is unreachable, you can still log in with a local account."},{"q":"Does the switch itself check the user's password during 802.1X?","a":"No -- it only relays EAP messages between the supplicant and the authentication server inside RADIUS; the server performs all the validation."},{"q":"A device passed 802.1X (Authz Success) but stayed in the default VLAN instead of the expected one. Where do you look first?","a":"First on the RADIUS server -- whether it's sending the Tunnel-Type/Tunnel-Private-Group-ID attributes in the Access-Accept; then on the switch -- whether the specified VLAN exists."},{"q":"The TACACS+ server went down, and no administrator can log in to the switches. What's missing from aaa authentication login?","a":"The local fallback method at the end of the list -- without it, the server being unreachable blocks login for everyone, including administrators."}]}]}],"qs":[231,232,251,253,258,294,312,335,602,606,612,614,660,662,667,672,676,677,682,687,688,698,729,883,975,989,992,1034,1130,1363]}