{
  "name": "ccna-exam-simulator",
  "version": "1.0.0",
  "description": "Offline CCNA 200-301 exam simulator — static web app + Android wrapper",
  "private": true,
  "scripts": {
    "sync-www": "node scripts/build-www.js",
    "android:sync": "npm run sync-www && npx cap sync android",
    "android:open": "npm run android:sync && npx cap open android"
  },
  "license": "UNLICENSED",
  "dependencies": {
    "@capacitor/android": "7.6.8",
    "@capacitor/cli": "7.6.8",
    "@capacitor/core": "7.6.8"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5"
  }
}
