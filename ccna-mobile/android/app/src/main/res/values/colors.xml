<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Same background token as the web layer, so the splash and the first painted frame
         match and there is no flash of a different colour on launch. The glyph's own two
         colours live in the drawable, which brand/generate.py writes from the mark's
         geometry — there is no second place to keep them in step. -->
    <color name="splashBackground">#0e1013</color>
</resources>
